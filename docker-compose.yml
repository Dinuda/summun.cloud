services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: summun
      POSTGRES_PASSWORD: summun
      POSTGRES_DB: summun
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U summun -d summun"]
      interval: 2s
      timeout: 5s
      retries: 30
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  server:
    build: .
    ports:
      - "3100:3100"
    environment:
      DATABASE_URL: postgres://summun:summun@db:5432/summun
      PORT: "3100"
      SERVE_UI: "true"
      PAPERCLIP_DEPLOYMENT_MODE: "authenticated"
      PAPERCLIP_DEPLOYMENT_EXPOSURE: "private"
      PAPERCLIP_PUBLIC_URL: "${PAPERCLIP_PUBLIC_URL:-http://localhost:3100}"
      BETTER_AUTH_SECRET: "${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET must be set}"
    volumes:
      - summun-data:/paperclip
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
  summun-data:
